


```bash
齿距 P = 2.00 mm
总共25个齿距 -> 总行程 `25 * 2.0 = 50.00 mm`
```
# 最终目标：精确计算向下按压的深度(单位：mm)
我们使用 4 * 正交编码，所以：
1个齿距 -> 4个脉冲
1个脉冲 -> 0.5mm


```C
深度 (mm) = depth_count × (齿距) / 4
         = depth_count × 2.00 / 4
         = depth_count × 0.5
```